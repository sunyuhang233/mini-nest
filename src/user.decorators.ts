// import { createParamsDecorator } from "@nestjs/common";

// export const User = createParamsDecorator((data, ctx: any) => {
//   const request = ctx.switchToHttp().getRequest();
//   return data ? request.user[data] : request.user
// })
